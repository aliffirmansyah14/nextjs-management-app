import LogoutButton from "@/features/auth/components/LogoutButton";

export default function DashboaradHeader() {
	return (
		<div>
			<LogoutButton variant="destructive">Logout</LogoutButton>
		</div>
	);
}
